node_modules
.next
out
build
.DS_Store
*.pem
.env
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
coverage

# Keel per-run gate result — machine-local, never committed.
.keel/gate-state.json

# Wrangler build/dry-run output
.wrangler/

# Operator export token — a secret, never committed.
.keel/export-token.txt
